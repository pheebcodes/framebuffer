{
	"name": "@phoebecodes/framebuffer",
	"version": "0.1.1",
	"description": "A library for handling length-prefixed frames.",
	"main": "index.cjs",
	"module": "index.js",
	"author": "phoebe <me@phoebe.codes>",
	"license": "CC-BY-SA-4.0",
	"repository": "github:pheebcodes/framebuffer",
	"scripts": {
		"pretest": "npm run build",
		"prepublishOnly": "npm run build && npm test",
		"test": "jest",
		"build": "esbuild index.js --bundle --platform=node > index.cjs"
	},
	"devDependencies": {
		"esbuild": "^0.17.19",
		"jest": "^23.6.0",
		"prettier": "^2.8.8"
	}
}
